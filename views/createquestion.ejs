<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>New question</title>
</head>
<body>
    <div class="grid grid-cols-3">
        <div class="col-start-2 colspan-3">
            <h1 class="text-2xl font-semibold text-black-600">
                Add a new Question
            </h1>
                <form action="/questionscreate/<%= id %>" method="post" class="px-8 py-6">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

                    <div class="py-3">
                    <label for="questionname">
                    Question</label>
                    <input id="questionname" name="questionname" 
                    type="text" placeholder="Question" 
                    class="border border-gray-300 rounded text-gray-900 w-full p-2">
                    </div>

                    <div class="py-3">
                    <label for="description" >
                    Description</label>
                    <textarea id="description" name="description" 
                    class="border border-gray-300 rounded text-gray-900 w-full p-2">
                    </textarea>
                    </div>

                    <div class="py-3">
                    <button type="submit" 
                    class="bg-green-600 text-white px-5 py-2 rounded font-medium mr-2 mb-2">
                    create</button>
                    <a href="/questions/<%=id%> " 
                    class="bg-red-600 text-white px-5 py-2.5 rounded font-medium ml-10 mb-2">
                    Cancel</a>
                    </div>

                </form>
            
                <div class="py-3">
                <% if(messages && messages.error && messages.error.length > 0)  { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <div class=" text-red-600">
                            <li> <%= messages.error[i] %></li>
                        </div>
                    <% } %>
                <% } %>
            </div>    
        </div>
    </div>
</body>
</html>